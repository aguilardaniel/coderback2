<div class="container my-5">
  <div class="row align-items-start">
    
    <!-- Imagen del producto -->
    <div class="col-md-6 mb-4 mb-md-0 text-center">
      <img src={{product.image}} alt={{product.title}} class="img-fluid rounded shadow" style="max-height: 450px; object-fit: cover;">
    </div>
    
    <!-- Información del producto -->
    <div class="col-md-6">
      <a href="/" class="text-primary d-block mb-3">Ver Todos los Productos</a>
      
      <div class="p-4 bg-white border rounded shadow-sm">
        <h4 class="fw-bold mb-2">{{product.title}}</h4>
        <p class="text-muted mb-3">{{product.description}}</p>
        <p class="h5 fw-bold text-dark mb-4">USD {{product.price}}</p>
        
        <button type="button" class="btn btn-success fw-semibold px-4 py-2">Agregar al Carrito!</button>
      </div>
    </div>
    
  </div>
</div>












{{!-- </main> --}}

